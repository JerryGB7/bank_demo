Terminal commands used in this project
===================================

This is a cleaned list pulled from the PowerShell history for the bank_demo project.

1. python -m venv .venv
2. .\.venv\Scripts\Activate.ps1
3. pip install -r requirements.txt
4. python -m pip install --upgrade pip
5. fastapi dev app/main.py
6. python -m scripts.day3_create_tables
7. python -m scripts.day5_seed_users
8. psql -U postgres -c "CREATE DATABASE bank_db"
9. psql -U postgres -d bank_db
10. cd backend
11. cd ..\db\sql
12. cd ..\..\backend
13. cd frontend
14. npm --version
15. npm install @mui/material @emotion/react @emotion/styled @mui/icons-material
16. npm create vite@latest frontend -- --template react
17. npm run dev
18. git config --global user.email "jerrygonzalezb7@gmail.com"
19. git config --global user.name "JerryGB7"
20. Invoke-WebRequest -Uri http://127.0.0.1:8000/health -UseBasicParsing | Select-Object -ExpandProperty Content
21. Invoke-WebRequest -Uri http://localhost:5173 -UseBasicParsing | Select-Object -ExpandProperty StatusCode
22. $login = Invoke-RestMethod -Uri http://127.0.0.1:8000/auth/token -Method Post -ContentType 'application/x-www-form-urlencoded' -Body 'username=manager&password=adminpass'; $login.access_token; Invoke-RestMethod -Uri http://127.0.0.1:8000/atms -Headers @{ Authorization = "Bearer $($login.access_token)" } | ConvertTo-Json -Depth 5

Additional setup / cleanup commands observed in history:

- Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
- Remove-Item -Recurse -Force .venv
- clear
- ls
- cd ..
- cd ..\
- cd "C:\Users\jerry\OneDrive\Desktop\bank_demo-main\backend"
- cd "C:\Users\jerry\OneDrive\Documents\GitHub\bank_demo"

Notes:
- Some earlier entries were from older project folders and were excluded from the cleaned list above.
- Repeated or typoed commands were condensed for readability.
